{
  "name": "open-feed-sorter",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source browser extension that sorts Instagram and TikTok profile feeds by views, likes, comments, date or outlier score \u2014 and exports the results.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node --test",
    "clean": "rm -rf dist",
    "verify": "node scripts/verify-dist.mjs",
    "icons": "node scripts/make-icons.mjs",
    "ci": "npm test && npm run build && npm run verify"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jsdom": "^30.1.0"
  }
}
